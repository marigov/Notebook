language: python # => 1
python: # => 2
  - "2.7"
services: # => 3
  - mysql
env: # => 4
  -DJANGO=1.10.11 DB=mysql
install: # => 5
  - pip install -r requirements.txt
script: # => 7
  - python manage.py test
